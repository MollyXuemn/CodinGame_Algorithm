/**
 * Reducer:
 * 
 * 1. GithubContext: you have some kind of functions in your Context, that will
 * dispatch some kind of action to your Reducer => your Reducer looks that action.type 
 * and update the state
 * case 'GET_USERS': 
    * return({...state,  
    *        users: action. payload,
    *        loading: false,})
    * 
    * then any components use that part of state from useContext that going to render it in the screen
 * 
 */

/**
 * How we can use the multiple context?
 * 
 * 
 * 
 */